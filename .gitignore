.env
docker/ca-certs/*.crt
.venv/
__pycache__/
*.pyc
.pytest_cache/
data/
*.duckdb
tableau/extract/
airflow/logs/
minio-data/
postgres-data/
.DS_Store

# Tableau Desktop's transient lock/recovery file, created alongside a .twb
# while it's open and normally cleaned up on close -- not meant to be
# versioned (same idea as Office's ~$file.docx lock files).
tableau/~*.twbr
