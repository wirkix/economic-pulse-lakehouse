# Installed on top of the apache/airflow base image (docker/airflow.Dockerfile).
# PyPI only, on purpose — see CLAUDE.md "Docker build network calls" gotcha.
pyspark==3.5.3
boto3==1.35.36
duckdb==1.1.3
requests==2.32.3
python-dotenv==1.0.1
pytest==8.3.3
